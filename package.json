{
  "name": "chalk-app",
  "version": "0.0.1",
  "private": true,
  "description": "Splitwise for India — keep score, stay even.",
  "workspaces": ["mobile", "web", "backend", "shared"],
  "scripts": {
    "backend": "bun run --filter @chalk/backend dev",
    "web": "bun run --filter @chalk/web dev",
    "mobile": "bun run --filter @chalk/mobile start"
  }
}
