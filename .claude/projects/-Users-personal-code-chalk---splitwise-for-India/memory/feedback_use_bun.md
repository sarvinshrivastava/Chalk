---
name: Use bun as package manager
description: User prefers bun over pnpm/npm for package management
type: feedback
---

Use bun instead of pnpm/npm as the package manager for this project.

**Why:** User explicitly asked to switch from pnpm to bun.
**How to apply:** All install, run, and workspace commands should use `bun` CLI.
